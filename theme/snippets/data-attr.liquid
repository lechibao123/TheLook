data-nav		= "{{ section.settings.nav | default: false }}"
data-bullet 	= "{{ section.settings.dot | default: false }}"
data-item-desk	= "{{ _item_per_row }}"
data-item-tab	= "{{ _item_per_row_tablet }}"
data-item-mob	= "{{ _item_per_row_mobile }}"
data-pad		= "{{ _padding }}"
data-autoplay 	= "{{ section.settings.autoplay | default: false }}"
data-loop		= "{{ section.settings.loop | default: false }}"